// Definition of the data record (struct/class with attributes).  
#include <stdio.h>
#include <bits/stdc++.h>
using namespace std;

class Record {
    private:
        int id;
        string name;

    public:
        Record(){};

        Record(string name){
            this -> name = name;
        }

        void Delete(int id){
            
        }

};